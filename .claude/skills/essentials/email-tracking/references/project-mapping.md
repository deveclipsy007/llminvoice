# Mapeamento do projeto

- Rotas de tracking em `config/routes.php`.
- Persistencia em tabela `emails` com `tracking_id`, `opened_at`, `clicked_at`.
- Controller atual: `src/Controllers/Admin/EmailController.php`.

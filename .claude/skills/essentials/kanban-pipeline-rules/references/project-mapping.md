# Mapeamento do projeto

- Endpoints kanban: `/api/kanban/columns`, `/api/kanban/move`, `/api/kanban/reorder`.
- Regras persistidas em `pipeline_transition_rules`.
- Estados de cliente em `clients.pipeline_column_id` e `position_in_column`.

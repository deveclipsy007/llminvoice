# Mapeamento do projeto

- Rotas: `POST /api/proposals/{clientId}`, `GET /api/proposals/{id}/versions`
- Estrutura: `proposals.current_version_id` -> `proposal_versions.id`
- Entidades auxiliares: `proposal_phases`, `proposal_deliverables`
